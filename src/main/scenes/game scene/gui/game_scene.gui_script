local GameGUISystem = require('src.systems.game.gui_system')

function init(self)
    GameGUISystem:init()
end

function on_message(self, message_id, message)
    GameGUISystem:on_message(message_id, message)
end

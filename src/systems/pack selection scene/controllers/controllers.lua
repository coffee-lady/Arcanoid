local Controllers = {
    localization = require('src.systems.pack selection scene.controllers.localization.localization'),
    transitions = require('src.systems.pack selection scene.controllers.transitions.transitions')
}

return Controllers

local CommonClasses = {
    boost = require('src.common.classes.boost'),
    go_system = require('src.common.classes.go_system'),
    gui_system = require('src.common.classes.gui_system'),
    localization = require('src.common.classes.localization')
}

return CommonClasses

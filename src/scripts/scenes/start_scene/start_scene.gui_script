local App = require('src.app')
local StrategiesMediator = require('src.scripts.scenes.start_scene.strategies.mediator')

local Scene = App.libs.scenes.Scene

local RENDER_ORDER = App.config.render_order.start_scene

local StartScene = class('StartScene', Scene)

function StartScene:initialize()
    Scene.initialize(self, StrategiesMediator, RENDER_ORDER)
end

StartScene()
